# ============================================
# A.Cosmos Environment Variables
# Copy this file to .env and fill in your values
# ============================================

# --- OpenAI API (Primary AI Provider) ---
# Your OpenAI API key — powers ALL Guardian AI conversations
# Get your key at: https://platform.openai.com/api-keys
# Supports both variable names (OPENAI_API_KEY takes priority)
OPENAI_API_KEY=sk-your-openai-api-key-here
# OpenAIAPIKey4Manus=sk-your-openai-api-key-here  # Legacy name, also supported

# --- Database ---
# PostgreSQL connection string (from Neon)
# Format: postgresql://user:password@host/database?sslmode=require
DATABASE_URL=postgresql://user:password@ep-xxx-xxx-123456.us-east-2.aws.neon.tech/neondb?sslmode=require

# --- Authentication ---
# Secret key for signing JWT tokens (generate a random 32+ character string)
JWT_SECRET=your_jwt_secret_here

# OAuth server URL (Phase 2: will be replaced with Google OAuth)
OAUTH_SERVER_URL=https://your-oauth-server.com

# Application ID (Phase 2: will be replaced with Google Client ID)
VITE_APP_ID=your_app_id_here

# Owner's OpenID (admin user identifier)
OWNER_OPEN_ID=your_open_id_here

# --- Forge API (Legacy — Phase 3 migration pending) ---
# These are only needed for auxiliary features: image generation, voice transcription,
# maps, storage, and notifications. All features gracefully degrade when not configured.
# BUILT_IN_FORGE_API_URL=https://your-forge-api-url.com
# BUILT_IN_FORGE_API_KEY=your_forge_api_key_here

# --- Runtime ---
NODE_ENV=development
